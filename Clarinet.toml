[project]
name = "reveal-store"
authors = ["Marine Data Research Team"]
description = "A decentralized platform for secure and transparent data revelation and access"
telemetry = false
requirements = []
[contracts.reveal-store]
path = "contracts/reveal-store.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
